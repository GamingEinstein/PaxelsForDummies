modLoader = "javafml"
loaderVersion = "[0,)"
license = "${license}"

[[mods]]
    modId = "${mod_id}"
    version = "${mod_version}"
    displayName = "${mod_name}"
    displayURL = "https://modrinth.com/mod/paxels-for-dummies"
    issueTrackerURL = "https://github.com/GamingEinstein/PaxelsForDummies/issues"
    logoFile = "icon.png"
    credits = "${credits}"
    authors = "${authors}"
    description = '''${mod_description}'''
    itemIcon = "paxelsfordummies:netherite_paxel"

[[dependencies."${mod_id}"]]
    modId = "neoforge"
    type = "required"
    versionRange = "[${neoforge_version},)"
    ordering = "NONE"
    side = "BOTH"

[[dependencies."${mod_id}"]]
    modId = "minecraft"
    type = "required"
    versionRange = "${minecraft_version_range}"
    ordering = "NONE"
    side = "BOTH"
